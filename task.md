# 饮料种类识别与无人售货机 —— 项目整体方案与第一阶段开发大纲

| 项目 | 内容 |
|---|---|
| 项目名称 | 基于 YOLO 的货架饮料识别与自助结算售货机 |
| 适用阶段 | 第一阶段：整体方案 + 框架搭建（**不含模型训练**） |
| 文档性质 | 需求评审结论 + 系统方案 + 开发 TODO 大纲（供小组/老师审核） |
| 关联文档 | `第25小组_饮料种类识别与无人售货机_需求跟踪矩阵.xlsx`（本阶段不修改） |
| 版本 | v1.0（2026-09-09） |
| 编制 | 第25小组 |

> 阅读指引：本文件分为 10 个部分。§1–§2 回答"需求要不要改、怎么改"（客观评审）；§3–§6 回答"系统怎么做"（架构/数据/识别引擎/接口）；§7–§8 回答"先做什么、做到什么程度算完成"（TODO 与验收）；§9 列风险与开放问题；§10 为将来回填需求矩阵的映射草稿。

---

## 1. 现有需求矩阵客观评审

### 1.1 评审视角

评审不照搬原文档条目，而是**以真实售货场景为基准**回答三个问题：

1. 现有条目描述的业务流程是否与"顾客从货架取走饮料、系统算账"的真实场景一致？
2. 支撑该场景的数据链路（识别 → 会话 → 库存 → 结算 → 预警）是否成环？
3. 需求是否可验收、口径是否明确（尤其"识别准确率"类指标）？

### 1.2 主要问题清单

| 编号 | 位置 | 问题描述 | 影响 | 建议 |
|---|---|---|---|---|
| P1 | 封面 / 设计用RTM 首行 | 项目名称写为"旧衣回收碳减排追踪与积分商城系统"，与文件名"饮料种类识别与无人售货机"矛盾 | 高（文档可信度） | 更正为统一项目名 |
| P2 | 主流程（原 No.1-24） | 核心交互是"单瓶拍照 → 手动加入购物车 → 去结算"，本质是**扫码购物**而非售货机**开/关门会话式识别** | 高（方向不符） | 重构为"开门 → 连续识别 → 关门自动结算" |
| P3 | 缺失 | 无"拿走/移除判定"需求——真实场景里最关键的是"顾客拿了什么、拿了几件" | 高 | 新增"会话内移除量统计"条目 |
| P4 | 缺失 | 无"关门事件驱动结算"——金额须在关门时由系统自动按（品类×单价）汇总 | 高 | 新增"关门自动出结算单"条目 |
| P5 | 缺失 | 识别记录与订单/会话无关联模型，无法回答"哪次交易识别了哪些帧" | 高 | 新增"会话(Session)主实体"及"会话明细/识别日志"关联条目 |
| P6 | 缺失 | "货位为空 → 提示补货"无对应需求（现有仅手动补货按钮） | 高 | 新增"低库存/空位预警链路"；阶段一按货道库存口径实现，预留视觉空位检测接口 |
| P7 | 缺失 | 无"后端摄像头采集 + 实时预览"条目（原条目是拍照单帧） | 中 | 新增"后端 OpenCV 采集 + MJPEG/预览流" |
| P8 | 原 No.1-5、6-12、25-28 | 手机号注册/登录、我的订单、购物车/清空等面向"顾客账号"功能 | 中 | 按"精简自助式"裁剪；订单体系收敛为"会话结算单"，保留管理员账号 |
| P9 | 原 No.33-40 | 图表/指标类（总销售额、总订单数、销售趋势折线、品类占比饼图、识别日志表）与需求吻合 | 低 | 保留并细化口径；"7 天销量曲线"明确为近 7 天销售额曲线 |
| P10 | 原 No.36 | "识别准确率 = 正确识别数/总识别数"缺真值来源，阶段一无标注数据无法按该口径计算 | 中 | 明确定义"演示期口径"（见 §5.5），后续由训练真值替换 |

### 1.3 "增 / 删 / 改"建议汇总

- **增加（新增需求）**：会话式识别主流程（开/关门）、移除量统计、关门自动结算、会话-明细-识别日志关联、低库存/空位预警、后端摄像头采集与预览、识别引擎 Provider 切换与训练接口、演示场景注入（验收用）。
- **修改（改口径）**：识别按钮"拍照单帧" →"会话流式捕获"；库存管理增加"补货阈值自动预警"与阈值可配；统计口径明确时间范围与金额/数量口径；识别准确率改为可演示口径。
- **删除/降级**：顾客注册/登录、购物车、清空购物车、立即购买跳转等条目从顾客主流程移除；原"主界面/购物车/订单记录"三块界面重构为"顾客操作台 + 管理员后台"两套界面。

### 1.4 一致性结论

原矩阵约 60% 条目（UI 组件、后台图表、库存表格等）可复用，但**核心业务流程与数据模型需重构**；建议将评审结论回填矩阵（新增"会话式识别"子模块、登记变更履历），**具体回填由小组确认后再执行**，本阶段不改表。

---

## 2. 已确认需求决策（本方案的评审口径）

| 编号 | 决策 | 说明 |
|---|---|---|
| D1 | 顾客精简自助式 | 顾客免注册登录：开门 → 取物 → 关门 → 自动出结算单 → 模拟支付 |
| D2 | 双角色 | 顾客操作台（一台演示 PC）+ 管理员后台（需登录），本地网页访问 |
| D3 | 技术栈 | FastAPI + SQLite + Vue3 + ECharts + ultralytics(YOLOv8) + OpenCV |
| D4 | 摄像头形态 | 后端 OpenCV 独占采集电脑摄像头；前端实时预览并下发开/关门控制 |
| D5 | 识别双通道 | 默认"模拟识别事件"通道（确定性、可复现演示）；"YOLOv8 预训练权重 + 摄像头"真跑通道（演示链路，不追求精度）；可切换 |
| D6 | 不训练模型 | 阶段一不训练、不标注；但**打通训练接口与目录规范**，待数据集就绪后可无缝切换自定义权重 |
| D7 | 补货判定 | 阶段一以"货道库存为唯一事实源"：成交扣库存 → 低于阈值即预警；预留视觉空位检测接口，不做深度视觉判断 |
| D8 | 单机单会话 | 摄像头/识别会话为单机单实例，后端串行控制，避免并发竞争 |

---

## 3. 总体方案

### 3.1 系统目标

模拟一台"带摄像头的自助饮料售货机"：顾客开门自助取物，系统通过识别引擎统计拿走品类与数量，关门自动按单价结算；货道低库存自动提醒补货；后台提供近 7 天销量曲线等运营视图。**第一阶段目标是把整条业务链路跑通且可演示、可验收。**

### 3.2 核心状态机（会话 Session）

```
idle ──POST /start──▶ opened（摄像头/模拟流开始，连续产生识别事件）
opened ──POST /stop──▶ closed（按会话内净拿走量生成结算单：status=unpaid，并扣库存、生成预警）
closed ──POST pay──▶ paid（模拟支付完成，写支付时间）
closed/paid ──POST /reset 或新 /start──▶ idle（新一轮）
```

### 3.3 总体架构图

```mermaid
flowchart LR
    subgraph FE["Vue3 前端（浏览器，127.0.0.1 本地访问）"]
        A1["顾客操作台<br/>预览 / 开门 / 关门 / 结算单 / 模拟支付"]
        A2["管理员后台<br/>登录 → 概览 / 库存补货 / 预警 / 记录 / 图表"]
    end
    subgraph BE["FastAPI 后端（Uvicorn）"]
        B1["路由层<br/>camera / session / inventory / admin / stats / model"]
        B2["领域服务<br/>会话状态机 · 移除结算 · 库存扣减 · 预警 · 统计 · 鉴权"]
        B3["识别引擎调度（Provider 工厂，可切换）"]
        B4["训练/数据文件访问<br/>configs / data / models"]
    end
    subgraph ENG["识别引擎 Provider 层"]
        C1["SimulatedProvider（默认，确定性演示）"]
        C2["YoloCameraProvider<br/>OpenCV 采集 + ultralytics 预训练权重"]
    end
    subgraph DB[("SQLite")]
        D1["product · lane · session · session_item<br/>recognition_log · restock_order · alert · admin_user"]
    end
    FE --> BE
    B1 --> B2
    B2 --> B3
    B3 --> ENG
    ENG --> B3
    B2 --> DB
    B4 --> DB
```

> 摄像头**只由后端进程独占采集**，预览经 MJPEG 推给前端；检测线程与预览/控制线程分离，避免阻塞控制接口。

### 3.4 模块划分

| 模块 | 职责 | 说明 |
|---|---|---|
| 会话模块 | 状态机、开/关门、结算单生成 | 核心闭环 |
| 识别引擎 | Provider 抽象/工厂/切换、模拟通道、YOLO 摄像头通道 | 业务与模型解耦 |
| 库存与补货 | 货道库存、成交扣减、补货登记、预警生成 | 事实源口径 |
| 统计模块 | 近 7 天销售额/销量曲线、品类销售占比、识别准确率口径 | 驱动后台图表 |
| 管理模块 | 管理员登录鉴权、商品/货道维护、预警处理 | JWT 会话 |
| 数据层 | SQLite 建表与种子数据、基础 CRUD | 初始化 6-10 种饮料演示数据 |

### 3.5 技术选型与部署形态

- 后端：Python 3.10+ / FastAPI / Uvicorn；SQLAlchemy(SQLModel 可选) + SQLite；OpenCV；ultralytics；密码 pbkdf2/bcrypt 哈希。
- 前端：Vue3 + Vite + ECharts + axios；预览用 `<img src="/api/camera/stream">`。
- 部署：FastAPI 托管前端 `dist` 静态资源实现**同源部署**（本地无跨域）；访问 `http://127.0.0.1:8000`，同局域网可经本机 IP 访问；相机默认 `camera_id=0` 可在配置中调整。

---

## 4. 数据模型设计（SQLite 表结构初稿）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `product` | id、name、brand、price(单价)、unit | 饮料品类主档 |
| `lane` | id、shelf_no(货架/货道编号)、product_id、capacity、stock(当前库存)、threshold(补货阈值) | 唯一事实源；`stock < threshold` 触发预警 |
| `session` | id、started_at、ended_at、status(open/closed/unpaid→closed后/paid)、total_amount、paid_at | 会话/交易主实体，每次开-关门一行 |
| `session_item` | id、session_id、product_id、quantity(净拿走量)、unit_price(成交时价)、subtotal | 结算明细 |
| `recognition_log` | id、session_id、ts、frame_no、provider、label、confidence、region、counted(是否计入成交) | 识别/模拟事件明细，支撑"识别日志"页 |
| `restock_order` | id、lane_id、product_id、quantity、operator、created_at | 手动补货登记流水 |
| `alert` | id、lane_id、type(low_stock/empty)、status(open/resolved)、message、created_at、resolved_at | 补货预警 |
| `admin_user` | id、username、password_hash、role | 仅管理员账号（简化自原登录体系） |

种子数据：内置管理员（初始账号口令在 README/配置中说明，首次登录提示修改）；6–10 种常见饮料（如可乐、冰红茶、矿泉水等）对应货道及单价，用于演示与验收。

---

## 5. 识别引擎与移除判定（阶段一落地口径）

### 5.1 Provider 抽象

```python
class DetectionProvider(ABC):
    def load(self, cfg) -> None: ...     # 加载权重/配置
    def detect(self, frame) -> list[Detection]: ...  # frame -> 检出项列表
    def status(self) -> dict: ...        # provider/模型/在线状态
# Detection: label, confidence, region(区域), ts
# 会话期间按 "开始帧基线 + 增量事件" 由 Provider 输出"拿走 X 商品 N 件"事件
```

### 5.2 SimulatedProvider（默认通道）

- 目的：让**业务闭环可确定性演示与验收**，不依赖模型能力。
- 机制：演示脚本（如"拿走 2 瓶可乐 + 1 瓶冰红茶"）在会话打开期间按时间节拍注入识别事件与日志；同时可配合"货道空位事件"触发预警演示。
- 用法：操作台提供"注入演示场景"按钮（`POST /api/demo/scenario`），管理员/演示者可一键复现标准场景。

### 5.3 YoloCameraProvider（真跑通道）

- 后端 OpenCV 逐帧取流（处理上限约 10–15fps），预览流与检测线程分离；检测结果写入识别日志，并驱动会话内的"出现/消失"统计做移除候选。
- 说明：受限于**未训练专用模型**，COCO 预训练权重只能检出通用物体（bottle/cup 等），本通道仅证明"摄像头 → 帧 → 检测 → 会话 → 日志"链路可用，**不做饮料品牌级判定**（待阶段二解决）。

### 5.4 关门结算规则（事实源口径）

1. `POST /api/session/stop` 触发：会话内识别事件按 `product` 汇总 → 得到**净拿走量**（口径 = 关门基线 − 开门基线，阶段一由 Provider 事件直接给出）。
2. 生成 `session`（status=closed/unpaid）+ `session_item`（数量 × 单价）。
3. 扣减对应 `lane.stock`；**扣减前校验库存，不允许负库存**（超出即提示并写入异常日志）。
4. 凡 `stock < threshold` 的货道生成 `alert(low_stock)`；若 Provider 上报空位事件则生成 `alert(empty)`。
5. 返回结算单（明细 + 总额）给操作台；顾客点"模拟支付"后置 `paid`。

### 5.5 识别准确率口径（演示期）

- 演示期口径：`统计周期内"置信度≥阈值 且 与场景真值一致"的成交识别事件数 ÷ 周期内成交识别事件总数`；由 SimulatedProvider 自带的场景真值支撑，避免无标注数据不可算。
- 阶段二模型训练并接入真值校验后，切换为"正确识别数 / 总识别数"，代码中预留口径接口，不写死。

### 5.6 训练接口预留（阶段一不训练）

- `models/`：权重产物目录（yolov8n.pt 预训练、自定义 `best.pt` 占位规范）。
- `data/drinks/`：YOLO 数据集规范目录（images / labels 与 `data.yaml`），**本阶段仅建目录与格式说明**。
- `configs/drinks.yaml`：**类别名 ↔ 商品 ↔ 单价映射**、置信度阈值、货架区域 ROI 映射——阶段一即建好，后续训练完只改权重路径即可。
- `scripts/train.py`：封装 ultralytics `train` 的骨架入口（数据路径、超参、输出目录从配置读取），本阶段只提供可执行骨架与说明，不实际训练。
- 约定：Provider 加载自定义权重后业务/结算代码**零改动**。

---

## 6. API 与前端页面清单

### 6.1 顾客操作台 API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/camera/stream` | MJPEG 实时预览流 |
| POST | `/api/session/start` | 开门，开始识别 |
| POST | `/api/session/stop` | 关门，返回结算单并扣库存/生成预警 |
| POST | `/api/session/{id}/pay` | 模拟支付，置 paid |
| POST | `/api/demo/scenario` | 注入模拟拿走清单（演示/验收用） |
| GET | `/api/model/status` | 当前识别通道与模型状态 |
| POST | `/api/model/switch` | 切换 Simulated / YOLO 通道（默认模拟） |

### 6.2 管理员 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/admin/login` | 管理员登录，发 JWT |
| GET | `/api/stats/overview` | 总销售额 / 总订单数 / 识别准确率（演示口径） |
| GET | `/api/stats/trend?days=7` | 近 7 天/30 天销售额折线（近 7 天默认） |
| GET | `/api/stats/category` | 品类销售数量占比（饼图） |
| GET/POST/PUT | `/api/inventory/*` | 货道与商品列表、新增/编辑（含阈值、单价） |
| POST | `/api/restock` | 补货登记（写 restock_order 并更新库存） |
| GET | `/api/alerts` / POST `/api/alerts/{id}/resolve` | 预警列表 / 处理 |
| GET | `/api/sessions` / `/api/sessions/{id}/logs` | 会话列表 / 某会话识别日志与明细 |
| GET | `/api/sessions/{id}/recognition` | 识别记录表格（时间、结果、置信度、是否成交） |

### 6.3 页面与交互流程

| 页面 | 关键内容 | 交互 |
|---|---|---|
| 顾客操作台（首页） | 摄像头预览、状态灯（已开门/识别中/已关门）、结算单弹层、模拟支付按钮、演示场景下拉 | 开始识别(开门) → 结束识别(关门) → 弹结算单 → 模拟支付 → 归位 |
| 管理员登录 | 用户名/密码 | 登录跳后台；后台接口均需带 token |
| 运营概览 | 指标卡（销售额/订单数/准确率）+ 近 7 天折线 + 品类占比饼图 | 刷新拉取 |
| 库存与补货 | 货道表格（stock/threshold 标色预警）、补货弹窗、商品维护 | 补货登记、调阈值/单价 |
| 预警与记录 | 预警列表（处理/忽略）、会话列表、会话详情含识别日志表格 | 下钻查看识别明细 |

---

## 7. 第一阶段开发 TODO 大纲与目录规划

### 7.1 目标目录结构（本轮仅规划，代码在后续轮次落地）

```
d:/yolo/task.md                        # 本轮交付：本方案文档
backend/
  requirements.txt
  app/main.py                          # FastAPI 入口，静态托管前端 dist
  app/core/config.py / security.py / db.py
  app/models/ *.py                     # 表模型（§4）
  app/routers/ session.py camera.py inventory.py admin.py stats.py model.py demo.py
  app/services/ session_svc.py settle.py inventory_svc.py alert_svc.py stats_svc.py
  engine/detector.py                   # Provider 抽象 + 工厂 + 状态
  engine/simulated.py
  engine/yolo_camera.py
  configs/drinks.yaml                  # 品类↔商品↔单价映射 + 阈值 + ROI
  data/drinks/data.yaml                # 数据集规范占位（阶段二填充）
  models/.gitkeep                      # 权重产物目录（阶段二落 best.pt）
  scripts/train.py                     # 训练骨架（预留，不执行）
  scripts/seed.py                      # 初始化管理员/商品/货道
frontend/
  package.json / vite.config.js
  src/main.js / router
  src/api/*.js
  src/pages/CashierView.vue            # 顾客操作台
  src/pages/AdminLogin.vue
  src/pages/Dashboard.vue              # 概览 + 图表(ECharts)
  src/pages/Inventory.vue              # 库存与补货
  src/pages/AlertsRecords.vue          # 预警与识别记录
docs/
  README.md                            # 启动步骤与演示剧本
```

### 7.2 开发 TODO 大纲（按后端 → 前端 → 联调排序）

| 序号 | TODO | 验收出口 |
|---|---|---|
| T01 | 初始化后端工程：FastAPI + SQLite(SQLAlchemy) + 配置 + CORS + 静态托管 | 服务可启动、健康检查通过 |
| T02 | 建表与种子数据（管理员/商品/货道），`seed.py` | 数据库初始化脚本幂等可重跑 |
| T03 | 识别引擎：Provider 抽象 + SimulatedProvider + 工厂 + `/api/model/status`、`/switch` | 可切换通道，模拟事件按场景产生 |
| T04 | 会话服务：start/stop/pay 状态机 + 关门生成结算单 + 扣库存 + 预警（负库存校验） | 见 §8 验收场景 S1 |
| T05 | 库存与补货 API + 预警处理 API | 补货登记后库存/预警正确变化 |
| T06 | 统计服务：近 7 天折线、品类占比、概览指标（含演示口径准确率） | 数据驱动图表正确 |
| T07 | 管理员鉴权（JWT）并保护全部管理接口 | 未登录访问被拒 |
| T08 | YoloCameraProvider：OpenCV 采集 + MJPEG 预览 + ultralytics 预训练推理线程 | 预览流畅、识别日志持续写入且不阻塞控制 |
| T09 | 前端骨架：Vite+Vue3+路由+axios+ECharts；顾客操作台（预览/开关门/结算/模拟支付） | S1 场景端到端跑通 |
| T10 | 前端：管理员登录 → 概览图表、库存补货、预警与识别记录页 | S2/S3 跑通 |
| T11 | 联调与演示剧本：一键初始化、确定性演示场景、README | 全链路演示 ≤5 分钟 |

---

## 8. 里程碑与验收标准

| 里程碑 | 内容 | 出口条件 |
|---|---|---|
| M1 方案定稿 | 本 task.md 通过小组/老师审核 | 评审意见收敛，进入编码 |
| M2 后端闭环 | T01–T07 完成 | 模拟通道端到端可跑通（无前端也可用 API/文档验证） |
| M3 前端完成 | T09–T10 完成 | 操作台与后台全部页面可用 |
| M4 摄像头通道 + 联调 | T08、T11 完成 | 双通道可切换演示，整体联调通过 |

**阶段一验收要点（演示剧本）**

- S1 模拟结算：注入"拿走 2 瓶可乐 + 1 瓶冰红茶"，关门后结算金额 = 2×可乐单价 + 1×冰红茶单价；库存相应扣减且不为负；低于阈值货道出现预警。
- S2 补货闭环：管理员登录 → 见预警 → 补货登记 → 库存恢复、预警可处理关闭。
- S3 后台图表：制造多笔会话后，近 7 天销售折线、品类占比饼图随数据变化；概览指标正确。
- S4 摄像头通道：切换 YOLO 通道后预览流畅（≥可辨识），持续产生识别日志，开/关门控制不被阻塞。
- S5 权限与健壮性：全部管理接口需登录；并发/重复 start 被拒绝；重复 stop/空会话有明确报错；断连/无摄像头有兜底提示并可回退模拟通道。

---

## 9. 风险、开放问题与阶段二衔接

### 9.1 风险清单

| 风险 | 说明 | 应对 |
|---|---|---|
| 无专用模型，品牌级识别不可用 | COCO 预训练无法区分可乐/冰红茶 | 阶段一以 SimulatedProvider 为验收口径；YOLO 通道仅证明链路；阶段二训练解决 |
| 识别准确率口径争议 | 无标注真值 | 采用演示期口径并文档声明（§5.5），接口预留真值校验 |
| 摄像头独占与冲突 | 相机被其他程序占用/权限问题 | camera_id 可配、启动自检并给出友好错误、可回退模拟通道 |
| 单会话串行限制 | 售货机形态天然单顾客 | 后端强制单会话，文档明示非目标（多机/多会话不做） |
| 拿错/放回等边界 | 顾客放回已拿商品 | 会话内"放回"以负增量事件表达（模拟通道已支持），净拿走量汇总口径已覆盖 |

### 9.2 开放问题（待小组确认，不阻塞开发）

1. 结算单的凭证形式：纯屏幕展示 + 模拟支付即可？是否需要打印/二维码样式？
2. 演示货架布局：实际货架是"层×列"还是"横排货道"？影响 `lane.shelf_no` 与 ROI 映射描述。
3. 需求矩阵是否回填及由谁维护（本组评审结论可作为回填依据）。

### 9.3 阶段二衔接（本次不做）

数据标注（自采饮料图集）→ 训练 YOLOv8（`scripts/train.py` + `data/drinks`）→ 替换自定义权重与映射 → 接入真值口径准确率 → 视觉空位检测深度实现 → 真实支付/打印等扩展。

---

## 10. 附录：需求矩阵回填映射草稿

> 仅用于后续回填参考（本阶段不改表）。"RTM No."沿用原设计用 RTM 编号，"状态"为建议动作。

| 建议动作 | 对象（原 RTM 条目 / 新条目） | 说明 |
|---|---|---|
| 修正 | 封面及首行项目名称 | 统一为"饮料种类识别与无人售货机"（P1） |
| 重构 | 主界面 No.14-18 / 购物车 No.20-24 / 订单 No.25-28 | 裁剪顾客账号/购物车，替换为会话式结算（P2、P8） |
| 新增 | 会话式识别主流程（子模块） | 开/关门 + 连续识别 + 移除统计 + 自动结算（P3、P4、P5） |
| 新增 | 摄像头采集与预览（子模块） | 后端 OpenCV + MJPEG（P7） |
| 新增 | 库存预警与补货（子模块） | 低库存/空位预警 + 补货登记（P6、D7） |
| 保留细化 | 数据统计 No.33-40 | 明确近 7 天口径与准确率演示口径（P9、P10） |
| 保留细化 | 库存管理 No.29-32 | 增加阈值字段与预警交互（P6） |

---

*—— 方案结束，供小组/老师审核。评审通过后按 §7 TODO 进入代码框架搭建。*
